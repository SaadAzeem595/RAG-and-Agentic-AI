## AI Nutrition Coach 🥗🤖
An AI-powered web application that analyzes food images to provide nutritional insights and calorie estimates using Generative AI and IBM watsonx.ai.

### 🌟 Features
Image Upload: Upload photos of your meals via a web interface.

AI Vision Analysis: Uses the vision-instruct model to identify food items.

Nutritional Insights: Get real-time estimates for calories and macronutrients.

### 📂 Project Structure
🚀 Setting up your development environment
Before diving into development, set up your project environment (optimized for Ubuntu 22.04 / Cloud IDE). This provides all the tools needed to build your AI-driven Flask application.

Step 1: Create your project directory
Open the terminal and run:

Step 2: Set up a Python virtual environment
Initialize and activate a new Python virtual environment:

Step 3: Install the library and packages
Install ibm-watsonx-ai to configure and utilize the vision-instruct model, along with other necessary packages:

###🛠️ Running the Application
Ensure your virtual environment is active:

Start the Flask server:

Open the provided URL (usually http://127.0.0.1:5000) in your browser to start coaching!
